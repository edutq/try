<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <SearchView
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/search_usuario"
        android:queryHint="Buscar Usuario..."
        style="@style/SearchCandados"/>
    <View style="@style/Divider"
        android:id="@+id/divider_dialog_usuario"
        app:layout_constraintTop_toBottomOf="@id/search_usuario"/>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:id="@+id/recylcer_dialog_usuario"
        app:layout_constraintTop_toBottomOf="@+id/divider_dialog_usuario"
        />

</androidx.constraintlayout.widget.ConstraintLayout>